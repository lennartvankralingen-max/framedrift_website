# Claude agent instructions (private)
CLAUDE.md

# Source .mov files (too large for GitHub, converted to MP4)
brand_assets/*.mov

# Source PDF (not used by website directly — rendered as JPEGs)
brand_assets/Discovery_Ultra_Styleguide_V1.pdf

# Intermediate compressed files (originals used instead)
brand_assets/*_compressed.mp4

# System
.DS_Store
.tmp/
